<Window x:Class="SecureMemo.Windows.PasswordMergeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="비밀번호 통합" Height="400" Width="500"
        WindowStyle="None" ResizeMode="NoResize"
        Background="#1a1a1a" AllowsTransparency="True"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style TargetType="Button" x:Key="ModernButton">
            <Setter Property="Background" Value="#2a2a2a"/>
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3a3a3a"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 타이틀바 -->
        <Border Grid.Row="0" Background="#1a1a1a" BorderBrush="#2a2a2a" BorderThickness="0,0,0,1"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <TextBlock Text="비밀번호 통합" Foreground="#888" FontSize="13" 
                           VerticalAlignment="Center" Margin="10,0"/>
                <Button Content="×" Width="46" Height="32" Click="CloseButton_Click"
                        HorizontalAlignment="Right" FontSize="16"
                        Style="{StaticResource ModernButton}"/>
            </Grid>
        </Border>

        <!-- 내용 -->
        <StackPanel Grid.Row="1" Margin="20">
            <TextBlock Foreground="#e0e0e0" FontSize="14" TextWrapping="Wrap" Margin="0,0,0,20">
                <Run Text="여러 비밀번호로 저장된 노트가 발견되었습니다."/><LineBreak/>
                <Run Text="통합할 비밀번호를 입력하세요."/>
            </TextBlock>

            <Border Background="#2a2a2a" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock x:Name="PasswordCountText" Foreground="#888" FontSize="13" Margin="0,0,0,12"/>
                    
                    <TextBlock Text="통합 비밀번호 입력" Foreground="#e0e0e0" FontSize="13" Margin="0,0,0,8"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <PasswordBox x:Name="MergePasswordBox" Grid.Column="0"
                                     Background="#1a1a1a" Foreground="#e0e0e0"
                                     BorderBrush="#3a3a3a" Padding="8" Height="36"
                                     KeyDown="MergePasswordBox_KeyDown"/>
                        <TextBox x:Name="MergePasswordTextBox" Grid.Column="0"
                                 Background="#1a1a1a" Foreground="#e0e0e0"
                                 BorderBrush="#3a3a3a" Padding="8" Height="36"
                                 Visibility="Collapsed"/>
                        <Button Grid.Column="1" Content="👁" Width="40" Height="36"
                                Click="TogglePasswordVisibility_Click"
                                Style="{StaticResource ModernButton}" Margin="4,0,0,0"/>
                    </Grid>
                </StackPanel>
            </Border>

            <TextBlock x:Name="StatusText" Foreground="#888" FontSize="12" 
                       Margin="0,0,0,12" TextWrapping="Wrap"/>

            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="통합" Click="Merge_Click"
                        Background="#6366f1" Foreground="White"
                        Style="{StaticResource ModernButton}" Margin="0,0,8,0"/>
                <Button Content="건너뛰기" Click="Skip_Click"
                        Style="{StaticResource ModernButton}"/>
            </WrapPanel>
        </StackPanel>
    </Grid>
</Window>
